explain select content from inodes join paths on paths.device=inodes.device and paths.inode=inodes.inode and paths.ctime=inodes.ctime where mode/4096=8 and path='/mnt/blacktar/luks/calendar' and not exists (select * from paths as alias where alias.path=paths.path and alias.xtime>paths.xtime and alias.xtime<1421295171);
explain select content from inodes join paths on paths.device=inodes.device and paths.inode=inodes.inode and paths.ctime=inodes.ctime where mode/4096=8 and path='/mnt/blacktar/luks/calendar' and xtime= (select max(xtime) from paths as alias where alias.path=paths.path and xtime<=1421295171);
